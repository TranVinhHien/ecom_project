# variables:
#     PROJECT_NAME: product_service
#     DOCKER_IMAGE: ${REGISTRY_USER}/${REGISTRY_PROJECT}/${CI_PROJECT_NAME}/${PROJECT_NAME}:${CI_COMMIT_TAG}_${CI_COMMIT_SHORT_SHA}
#     DOCKER_CONTAINER: ${CI_PROJECT_NAME}/${PROJECT_NAME}
#     HOST_PORT: "9001"
#     CONTAINER_PORT: "9001"
# stages:
#     - buildandpush
#     - deploy
#     - showlog

# buildandpush:
#     stage: buildandpush
#     variables:
#         GIT_STRATEGY: clone
#     before_script:
#         - docker login -u ${REGISTRY_USER} -p ${REGISTRY_PASSWORD}
#     script:
#         - docker build -t $DOCKER_IMAGE .
#         - docker push $DOCKER_IMAGE
#     tags:
#         - lab-server
#     only:
#         - tags

# deploy:
#     stage: deploy
#     variables:
#         GIT_STRATEGY: none
#     before_script:
#         - docker login  -u ${REGISTRY_USER} -p ${REGISTRY_PASSWORD}
#     script:
#         - docker pull $DOCKER_IMAGE
#         - docker rm -f $DOCKER_CONTAINER
#         - docker run --name $DOCKER_CONTAINER -d -p $HOST_PORT:$CONTAINER_PORT $DOCKER_IMAGE
#     tags:
#         - lab-server
#     only:
#         - tags

# showlog:
#     stage: showlog
#     variables:
#         GIT_STRATEGY: none
#     script:
#         - sleep 20
#         - docker logs $DOCKER_CONTAINER
#     tags:
#         - lab-server
#     only:
#         - tags
variables:
  PRODUCT_NAME: "api-order-service" 
  PRODUCT_IMAGE: ${REGISTRY_USER}/${PRODUCT_NAME}:${CI_COMMIT_TAG}_${CI_COMMIT_SHORT_SHA}
  PRODUCT_CONTAINER: ${PRODUCT_NAME}   
  PRODUCT_HOST_PORT: "9002"
  PRODUCT_CONTAINER_PORT: "9002"
  SERVICE_PATH: "api_system/ecom_order_service"

build_order:
  stage: buildandpush
  variables:
    GIT_STRATEGY: clone
  before_script:
    - docker login -u ${REGISTRY_USER} -p ${REGISTRY_PASSWORD}
  script:
    - echo "Building order Service..."
    - cd $SERVICE_PATH 
    - docker build -t $PRODUCT_IMAGE .
    - docker push $PRODUCT_IMAGE
  tags:
    - lab-server
  rules:
    - if: $CI_COMMIT_TAG
      changes:
        - api_system/ecom_order_service/**/*

deploy_order:
  stage: deploy
  variables:
    GIT_STRATEGY: none
  before_script:
    - docker login -u ${REGISTRY_USER} -p ${REGISTRY_PASSWORD}
  script:
    - docker pull $PRODUCT_IMAGE
    - docker rm -f $PRODUCT_CONTAINER || true
    - docker run --name $PRODUCT_CONTAINER \
      --env-file $ENV_FILE_ORDER \
      --restart unless-stopped \
      -d -p $PRODUCT_HOST_PORT:$PRODUCT_CONTAINER_PORT \
      $PRODUCT_IMAGE
  tags:
    - lab-server
  rules:
    - if: $CI_COMMIT_TAG
      changes:
        - api_system/ecom_order_service/**/*

showlog_order:
  stage: showlog
  variables:
    GIT_STRATEGY: none
  script:
    - sleep 10
    - docker logs $PRODUCT_CONTAINER
  tags:
    - lab-server
  rules:
    - if: $CI_COMMIT_TAG
      changes:
        - api_system/ecom_order_service/**/*